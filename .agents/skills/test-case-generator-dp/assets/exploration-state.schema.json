{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "断点续传进度文件结构。Phase 3 每完成一个区域，AI 追加写入 OUTPUT_DIR/.exploration-state.json；重新启动技能时优先读取，向用户汇报'上次探索到 X，是否继续'。",
  "title": "ExplorationState",
  "type": "object",
  "required": ["module", "covered_areas", "pending_areas", "last_updated"],
  "properties": {
    "module": {
      "type": "string",
      "description": "模块全名，如 '生产管理_制造排产'，与输出文件名一致"
    },
    "domain": {
      "type": "string",
      "enum": ["MOM", "ERP", "WMS"],
      "description": "领域，Phase 1 用户确认"
    },
    "vtable_columns_scanned": {
      "type": "boolean",
      "description": "VTable 列分类是否已完成（scanColumns 已运行）"
    },
    "covered_areas": {
      "type": "array",
      "items": { "type": "string" },
      "description": "已探索完毕的区域，枚举：筛选区 / 工具栏按钮 / VTable表头 / VTable行选择 / VTable链接列 / 页面级 / <按钮名>弹窗",
      "examples": [["筛选区", "工具栏_批量排产"]]
    },
    "pending_areas": {
      "type": "array",
      "items": { "type": "string" },
      "description": "尚未探索的区域，建议下次继续的入口"
    },
    "derived_cases_count": {
      "type": "integer",
      "description": "已生成的衍生用例累计条数（含未导出的）"
    },
    "last_updated": {
      "type": "string",
      "format": "date-time",
      "description": "最后更新时间（ISO 8601）"
    }
  }
}
